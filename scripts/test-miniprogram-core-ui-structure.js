const assert = require('assert');
const fs = require('fs');
const path = require('path');

function read(relativePath) {
  return fs.readFileSync(path.resolve(__dirname, '..', relativePath), 'utf8');
}

const home = read('miniprogram/pages/index/index.wxml');
const homeStyles = read('miniprogram/pages/index/index.wxss');
const coreActionScenes = read('miniprogram/utils/core-action-scenes.js');
assert.ok(coreActionScenes.includes("require('./core-action-age-catalog.js')"), 'age-first catalog require should use explicit .js extension for WeChat DevTools');
assert.ok(home.includes('class="core-hero"'), 'home should render core hero');
assert.ok(home.includes('按年龄找孩子问题'), 'home should explain the age-first parenting problem entry');
assert.ok(home.includes('学习专注、情绪规则、运动体能、社交表达'), 'home should expose miniprogram theme categories');
assert.ok(home.includes('class="core-hero" wx:if="{{coreRefactorEnabled}}"'), 'core hero should be gated by core refactor flag');
assert.ok(home.includes('bindtap="onHomePrimaryActionTap"'), 'home should expose primary action');
assert.ok(home.includes('wx:for="{{coreRefactorState.ageSegments}}"'), 'home should render age segments');
assert.ok(home.includes('ageFirstCoreEnabled'), 'age-first core entry should be gated by age-first flag');
assert.ok(home.includes('ageFirstCoreAvailable'), 'age-first core entry should be gated by usable age-first config');
assert.ok(home.includes('data-segment-key="{{item.key}}"'), 'age segment cards should pass segment key');
assert.ok(home.includes('bindtap="onCoreAgeSegmentTap"'), 'age segment cards should select an age segment');
assert.ok(home.includes('wx:for="{{coreScenes}}"'), 'home should keep scene-first core fallback scenes');
assert.ok(home.includes('data-scene-key="{{item.key}}"'), 'scene-first fallback should pass scene key');
assert.ok(home.includes('bindtap="onCoreSceneTap"'), 'scene-first fallback should select a scene');
assert.ok(home.includes('wx:for="{{coreRefactorState.ageOptions}}"'), 'scene-first fallback should render age options');
assert.ok(home.includes('bindtap="onCoreAgeTap"'), 'scene-first fallback should select age before symptoms');
assert.ok(home.includes('wx:for="{{coreRefactorState.featuredPainPoints}}"'), 'home should render featured age pain points first');
assert.ok(home.includes('<text class="core-pain-name">{{item.title}}</text>'), 'featured pain cards should render title');
assert.ok(home.includes('<text class="core-pain-desc">{{item.description}}</text>'), 'featured pain cards should render description');
assert.ok(home.includes('wx:for="{{item.observableSigns}}"'), 'featured pain cards should render observable signs');
assert.ok(home.includes('wx:for="{{item.abilityTags}}"'), 'featured pain cards should render ability tags');
assert.ok(home.includes('wx:for="{{coreRefactorState.painCategories}}"'), 'home should render categorized full age pain points');
assert.ok(home.includes('coreRefactorState.recentPainCategoryKey === category.key'), 'home should highlight recent pain category');
assert.ok(home.includes('data-category-key="{{category.key}}"'), 'category header should pass category key');
assert.ok(home.includes('bindtap="onCorePainCategoryTap"'), 'category header should save recent category');
assert.ok(home.includes('<text class="core-pain-category-title">{{category.label}}</text>'), 'full pain list should render category title');
assert.ok(home.includes('<text class="core-pain-category-desc">{{category.description}}</text>'), 'full pain list should render category description');
assert.ok(home.includes('wx:for="{{category.painPoints}}"'), 'full pain list should render category pain points');
assert.ok(home.includes('class="core-pain-item core-pain-item-compact'), 'full pain list should render compact pain point cards');
assert.ok(home.includes('bindtap="onToggleCorePainPointList"'), 'home should toggle full age pain point list');
assert.ok(home.includes('data-pain-point-key="{{item.key}}"'), 'pain point cards should pass pain point key');
assert.ok(home.includes('bindtap="onCorePainPointTap"'), 'pain point cards should select a pain point');
assert.ok(home.includes('coreRefactorState.observableSigns'), 'result should expose observable signs');
assert.ok(home.includes('coreRefactorState.abilityTags'), 'result should expose ability tags');
assert.ok(homeStyles.includes('.core-age-summary'), 'home should style age summary');
assert.ok(homeStyles.includes('.core-pain-item'), 'home should style pain point cards');
assert.ok(homeStyles.includes('.core-pain-category'), 'home should style categorized pain point list');
assert.ok(homeStyles.includes('.core-pain-category-active'), 'home should style recent pain category');
assert.ok(homeStyles.includes('.core-pain-more'), 'home should style pain point expand action');
assert.ok(homeStyles.includes('.core-ability-tag'), 'home should style ability tags');
assert.ok(homeStyles.includes('.core-result-observe'), 'home should style observable signs');
assert.ok(home.includes('data-symptom-key="{{item.key}}"'), 'symptom options should pass symptom key');
assert.ok(home.includes('data-effect-key="{{item.key}}"'), 'effect options should pass effect key');
assert.ok(home.includes('class="core-result-card"'), 'home should render first action result card');
assert.ok(home.includes('bindtap="onCoreAskDetailTap"'), 'result should include chat follow-up action');
assert.ok(home.includes('bindtap="onSaveCoreActionToGrowthRecordTap"'), 'result should include growth record action');
assert.ok(home.includes('class="core-membership"'), 'home should render core membership touchpoint');
assert.ok(home.includes('wx:if="{{coreRefactorEnabled && membershipTouchpointVisible}}"'), 'core membership should be gated by core refactor flag');
assert.ok(home.includes('class="core-tools" wx:if="{{coreRefactorEnabled}}"'), 'core tools should be gated by core refactor flag');
assert.ok(home.includes('wx:if="{{showLegacyHomeSections}}"'), 'home should keep legacy fallback sections');

const chatJs = read('miniprogram/pages/chat/chat.js');
const chatWxml = read('miniprogram/pages/chat/chat.wxml');
assert.ok(chatJs.includes('pendingCoreActionContext'), 'chat should read pending core context');
assert.ok(chatJs.includes('buildPendingCoreActionQuestion'), 'chat should build core context question');
assert.ok(chatJs.includes('年龄：'), 'chat follow-up should include child age context');
assert.ok(chatJs.includes('年龄段Key：'), 'chat follow-up should include age segment key context');
assert.ok(chatJs.includes('类别：'), 'chat follow-up should include category label context');
assert.ok(chatJs.includes('类别Key：'), 'chat follow-up should include category key context');
assert.ok(chatJs.includes('场景：'), 'chat follow-up should include family scene context');
assert.ok(chatJs.includes('痛点：'), 'chat follow-up should include pain point context');
assert.ok(chatJs.includes('表现：'), 'chat follow-up should include symptom context');
assert.ok(chatJs.includes('可观察表现：'), 'chat follow-up should include observable signs context');
assert.ok(chatJs.includes('背后能力：'), 'chat follow-up should include ability tags context');
assert.ok(chatJs.includes('卡点判断：'), 'chat follow-up should include bottleneck context');
assert.ok(chatJs.includes('今晚第一步：'), 'chat follow-up should include tonight action context');
assert.ok(chatJs.includes('孩子反应不同怎么办'), 'chat follow-up should ask for response-specific guidance');
assert.ok(chatWxml.includes('继续追问细节'), 'chat empty state should align with core follow-up copy');

const searchJs = read('miniprogram/pages/parenting/search/search.js');
assert.ok(searchJs.includes('sceneKey: sceneKey'), 'scene search should keep core scene key');
assert.ok(searchJs.includes('ageGroup: ageGroup'), 'scene search should keep child age group');
assert.ok(searchJs.includes('ageSegmentKey: ageSegmentKey'), 'scene search should keep core age segment key');
assert.ok(searchJs.includes('categoryKey: categoryKey'), 'scene search should keep core category key');
assert.ok(searchJs.includes('categoryLabel: categoryLabel'), 'scene search should keep core category label');
assert.ok(searchJs.includes('painPointKey: painPointKey'), 'scene search should keep core pain point key');
assert.ok(searchJs.includes('painPointTitle: painPointTitle'), 'scene search should keep core pain point title');
assert.ok(searchJs.includes('abilityTags: abilityTags'), 'scene search should keep core ability tags');
assert.ok(searchJs.includes("url: '/search/solutions'"), 'scene search should request structured solutions');
assert.ok(searchJs.includes("url: '/parenting/search'"), 'scene search should request article knowledge');

const serverJs = read('backend/src/mysql-production/server.js');
assert.ok(serverJs.includes('const references = await collectChatReferences'), 'chat backend should collect knowledge references before answering');
assert.ok(serverJs.includes('const coreActionContext = buildCoreActionChatContext(req, message)'), 'chat backend should build core action context before collecting references');
assert.ok(serverJs.includes('collectChatReferences(chatAnalysis, message, ageGroup, coreActionContext)'), 'chat backend should pass core action context into reference collection');
assert.ok(serverJs.includes('buildChatPrompt(message, chatAnalysis, references, ageGroup, chatChildContext, coreActionContext)'), 'chat backend should pass core action context into prompt building');
assert.ok(serverJs.includes('function appendCoreActionKeywords(keywords, coreActionContext)'), 'chat backend should expand keywords from core action context');
assert.ok(serverJs.includes("extractCoreActionLine(message, '年龄段Key')"), 'chat backend should parse age segment key from core follow-up text');
assert.ok(serverJs.includes("extractCoreActionLine(message, '类别Key')"), 'chat backend should parse category key from core follow-up text');
assert.ok(serverJs.includes("extractCoreActionLine(message, '类别')"), 'chat backend should parse category label from core follow-up text');
assert.ok(serverJs.includes("extractCoreActionLine(message, '痛点')"), 'chat backend should parse pain point from core follow-up text');
assert.ok(serverJs.includes("extractCoreActionLine(message, '背后能力')"), 'chat backend should parse ability tags from core follow-up text');
assert.ok(serverJs.includes("extractCoreActionLine(message, '可观察表现')"), 'chat backend should parse observable signs from core follow-up text');
assert.ok(serverJs.includes('核心行动上下文：'), 'chat prompt should include core action context block');
assert.ok(serverJs.includes('同能力维度的知识片段兜底'), 'chat prompt should fallback by age segment and ability dimension');
assert.ok(serverJs.includes('只允许基于参考资料回答'), 'chat prompt should constrain answers to references');
assert.ok(serverJs.includes('所有建议必须严格匹配'), 'chat prompt should constrain advice by age group');
assert.ok(serverJs.includes('reference_count'), 'chat analytics should record reference count');
assert.ok(serverJs.includes('buildCoreSearchContextFromQuery(req.query)'), 'search backend should parse core action query context');
assert.ok(serverJs.includes('buildCoreSearchKeywords(keyword, profile, coreSearchContext)'), 'search backend should expand recommendation keywords from core context');
assert.ok(serverJs.includes('coreSearchContext && coreSearchContext.categoryLabel'), 'search backend should expand keywords by category label');
assert.ok(serverJs.includes('coreActionContext: coreSearchContext'), 'search backend should return core action context');
assert.ok(serverJs.includes('normalizeSearchKeywordList(keyword)'), 'parenting article search should support multiple context keywords');

const growth = read('miniprogram/pages/growth-record/index.wxml');
assert.ok(growth.includes('empty-card'), 'growth record should include empty state');

const nutrition = read('miniprogram/pages/nutrition/nutrition.wxml');
const textbook = read('miniprogram/pages/textbook/textbook.wxml');
assert.ok(nutrition.includes('empty-wrap'), 'nutrition should include empty state');
assert.ok(textbook.includes('reading-empty-section'), 'reading practice should include empty state');

const weeklySummaryJs = read('miniprogram/pages/weekly-summary/index.js');
assert.ok(weeklySummaryJs.includes("wx.removeStorageSync('pendingCoreWeeklySummary')"), 'weekly summary should clear consumed core fallback summary');

console.log('Miniprogram core UI structure tests passed.');
