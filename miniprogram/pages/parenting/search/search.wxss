/* 搜索页面样式 */

.page {
  min-height: 100vh;
  background: #FAF8F5;
}

.retry-button { margin: 28rpx auto 0; padding: 16rpx 28rpx; border: 1rpx solid #6AAE98; border-radius: 24rpx; background: #6AAE98; color: #FFFFFF; font-size: 26rpx; line-height: 1.45; }
.partial-state { margin: 0 32rpx 20rpx; padding: 18rpx 22rpx; border: 1rpx solid #ECEFEB; border-radius: 20rpx; background: #E6F7ED; color: #397A68; font-size: 24rpx; line-height: 1.55; }

/* 搜索栏 */
.search-bar {
  padding: 16rpx 32rpx;
  background: #FFFFFF;
  border: 1rpx solid #ECEFEB;
  position: sticky;
  top: 0;
  z-index: 100;
}

.search-input-wrap {
  display: flex;
  align-items: center;
  background: #FAF8F5;
  border-radius: 40rpx;
  padding: 16rpx 24rpx;
}

.back-btn {
  font-size: 40rpx;
  color: #626A66;
  margin-right: 12rpx;
  padding: 0 8rpx;
}

.search-icon {
  font-size: 28rpx;
  margin-right: 12rpx;
}

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #2F3432;
}

.clear-btn {
  font-size: 28rpx;
  color: #929995;
  padding: 8rpx;
}

/* 搜索建议区域 */
.search-suggest {
  padding: 24rpx 32rpx;
}

.suggest-section {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.section-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #2F3432;
}

.clear-history {
  font-size: 26rpx;
  color: #929995;
}

.hot-tag {
  font-size: 28rpx;
}

/* 关键词列表 */
.keyword-list {
  display: flex;
  flex-wrap: wrap;
}

.keyword-item {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: #FAF8F5;
  border-radius: 32rpx;
  margin-right: 16rpx;
  margin-bottom: 16rpx;
}

.keyword-icon {
  font-size: 24rpx;
  margin-right: 8rpx;
}

.keyword-text {
  font-size: 26rpx;
  color: #626A66;
}

.hot-list .keyword-item {
  background: #F5E8BC;
}

.hot-list .keyword-text {
  color: #F28C72;
}

/* 推荐搜索 */
.recommend-list {
  margin-top: 8rpx;
}

.recommend-item {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
  border-bottom: 1rpx solid #FAF8F5;
}

.recommend-item:last-child {
  border-bottom: none;
}

.recommend-icon {
  font-size: 28rpx;
  margin-right: 12rpx;
}

.recommend-text {
  font-size: 28rpx;
  color: #2F3432;
}

/* 搜索结果 */
.search-results {
  padding: 24rpx 32rpx;
}

.scene-card,
.solution-card {
  background: #FFFFFF;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: none;
}

.scene-card-head {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
  margin-bottom: 16rpx;
}

.scene-category,
.solution-type {
  font-size: 22rpx;
  color: #6AAE98;
}

.scene-title,
.solution-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #2F3432;
}

.scene-copy,
.solution-summary {
  font-size: 26rpx;
  color: #626A66;
  line-height: 1.7;
}

.scene-copy-action {
  margin-top: 12rpx;
  color: #2F3432;
}

.solution-list {
  margin-bottom: 20rpx;
}

.result-count {
  font-size: 26rpx;
  color: #929995;
  margin-bottom: 20rpx;
}

/* 加载状态 */
.loading-wrap {
  display: flex;
  justify-content: center;
  padding: 60rpx 0;
}

.loading-text {
  font-size: 26rpx;
  color: #929995;
}

/* 空状态 */
.empty-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 0;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.empty-text {
  font-size: 28rpx;
  color: #626A66;
  margin-bottom: 8rpx;
}

.empty-desc {
  font-size: 24rpx;
  color: #929995;
}

/* 文章列表 */
.article-list {
  margin-top: 0;
}

.article-card {
  background: #FFFFFF;
  border-radius: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: none;
  overflow: hidden;
  position: relative;
}

.article-main {
  display: flex;
  padding: 24rpx;
}

.article-cover {
  width: 200rpx;
  height: 150rpx;
  border-radius: 16rpx;
  flex-shrink: 0;
  background: #FAF8F5;
}

.article-content {
  flex: 1;
  margin-left: 20rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.article-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #2F3432;
  line-height: 1.5;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.article-summary {
  font-size: 24rpx;
  color: #929995;
  line-height: 1.7;
  margin-top: 10rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.article-tags {
  display: flex;
  flex-wrap: wrap;
  margin-top: 12rpx;
}

.tag {
  display: inline-block;
  padding: 6rpx 16rpx;
  border-radius: 16rpx;
  font-size: 20rpx;
  line-height: 1.45;
  margin-right: 12rpx;
  background: #F3DFE3;
  color: #6AAE98;
}

.tag.tag-age {
  background: #F5E8BC;
  color: #F28C72;
}

.article-meta {
  display: flex;
  align-items: center;
  margin-top: 12rpx;
}

.meta-item {
  font-size: 22rpx;
  color: #929995;
  margin-right: 16rpx;
  line-height: 1.45;
}

/* 收藏按钮 */
.favorite-btn {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 50%;
  box-shadow: none;
}

.favorite-btn.active {
  background: #FFEBEE;
}

.favorite-icon {
  font-size: 32rpx;
}

/* 加载更多 */
.load-more {
  text-align: center;
  padding: 30rpx 0;
  font-size: 26rpx;
  color: #929995;
  line-height: 1.6;
}
