# 内容发布一致性修复

1. 当编辑已发布文章或痛点时，系统应保留线上版本直到新版本发布。
2. 当直接或定时发布文章时，系统应将目标版本正文与发布状态在同一事务内同步至文章表。
3. 当恢复历史版本后重新发布时，系统应展示所选历史内容。
4. 当运营人员打开内容列表时，系统应提供最新草稿及完整编辑字段。
5. 当正式知识查询到末页时，系统应返回空页；当使用本地回退时，系统应应用相同分页偏移。
6. 训练与营养运营应遵循各自业务字段及消费端数据源，已有内容标识保持稳定。
