# 需求实施计划

- [x] 1. 固化首页入口的自然跳转逻辑
  - [x] 1.1 保留首页无上下文入口跳转育儿锦囊的行为
    - 修改 `miniprogram/pages/index/index.js` 和 `miniprogram/pages/index/index.wxml`，确保无文章、测评或用户输入上下文时不写入 `pendingChatQuestion`
    - 首页卡片文案表达先看与当前孩子相近的问题，缺少档案时引导补全信息
    - 覆盖需求 R1.1、R1.2、R1.3、R1.4
  - [x]* 1.2 为首页入口行为编写回归测试
    - 校验 `askAiForToday()` 不再生成默认 AI 预填问题
    - 校验文章详情页等明确上下文入口仍可预填 AI 问题
    - 覆盖需求 R1.2、R5.1

- [x] 2. 统一孩子档案上下文构建
  - [x] 2.1 扩展小程序孩子上下文工具
    - 在 `miniprogram/app.js` 中新增或扩展统一函数，输出 `id`、`name`、`birthday`、`age_group`、`tags`、`concerns`、`source`
    - 复用现有 `normalizeChild()` 和年龄计算逻辑，缺少生日或年龄时返回空年龄段
    - 覆盖需求 R3.1、R4.1、R4.3
  - [x] 2.2 调整 AI 聊天请求上下文
    - 修改 `app.chat(message)`，把 `child_profile` 设置为统一孩子上下文
    - 保持接口路径、超时和消息截断逻辑不变
    - 覆盖需求 R3.1
  - [x]* 2.3 为小程序孩子上下文编写单元测试
    - 覆盖生日有效、生日缺失、标签数组和关注点字符串归一化
    - 覆盖缺失年龄不会生成 `6-9岁` 的性质
    - 覆盖需求 R4.1、R4.2、R4.3、R5.1

- [x] 3. 扩展后端 AI 档案上下文
  - [x] 3.1 扩展 `buildChatChildContext()` 输出字段
    - 修改 `backend/src/mysql-production/server.js`，让聊天上下文包含 `tags`、`concerns` 和来源
    - 新增轻量数组归一化函数，兼容数组、JSON 字符串和分隔字符串
    - 覆盖需求 R3.2、R3.3
  - [x] 3.2 把档案上下文注入 AI Prompt
    - 在聊天 Prompt 构建处追加简短孩子档案描述
    - 用户消息显式年龄优先，档案年龄作为补充，性格标签和关注点持续参与回答
    - 覆盖需求 R3.3、R3.5
  - [x] 3.3 保持年龄缺失澄清链路
    - 校验 `buildChatAgeClarificationAnswer()` 在档案缺少年龄时继续触发
    - 确保缺失年龄不会被映射为 `6-9岁`
    - 覆盖需求 R3.4、R4.1、R4.2、R4.3
  - [x]* 3.4 为后端上下文构建编写单元测试
    - 覆盖请求体档案、`child_id`、默认孩子档案的优先级
    - 覆盖标签和关注点归一化，以及缺失年龄兜底回答
    - 覆盖需求 R3.2、R3.3、R3.4、R5.1

- [x] 4. 接入育儿锦囊个性化推荐
  - [x] 4.1 抽取育儿年龄段推断能力
    - 将 `parenting.js` 中的 `inferChildAgeRange(child)` 抽到可复用位置，或在 `app.js` 中提供合法育儿年龄段函数
    - 只返回 `/parenting/articles` 支持的年龄段，无法匹配时返回空字符串
    - 覆盖需求 R2.1、R2.4、R4.4
  - [x] 4.2 在育儿锦囊首页展示推荐来源
    - 修改 `miniprogram/pages/parenting/parenting.js` 和对应模板样式，展示 `按孩子姓名 年龄段推荐` 或补全年龄提示
    - 请求文章时继续仅在合法年龄段存在时传 `age_group`
    - 覆盖需求 R2.1、R2.5、R4.4
  - [x] 4.3 在文章列表页默认选中当前孩子年龄
    - 修改 `miniprogram/pages/parenting/article-list/article-list.js`，普通入口根据当前孩子设置 `currentAge`
    - URL 参数和用户手动筛选优先级高于默认推荐
    - 覆盖需求 R2.2、R2.3、R2.5
  - [x]* 4.4 为育儿年龄推荐编写单元测试
    - 覆盖合法年龄段、超范围年龄、URL 参数优先级和手动筛选保持
    - 覆盖需求 R2.2、R2.3、R2.4、R4.4、R5.1

- [x] 5. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 6. 完成语法和回归校验
  - [x] 6.1 执行 JS 语法检查
    - 对修改过的后端和小程序 JS 文件运行 `node --check`
    - 覆盖需求 R5.2
  - [x] 6.2 执行项目级回归校验
    - 运行根目录 `npm test`，确保既有鼓励弹窗和新增上下文测试通过
    - 运行根目录 `npm run lint`，确保后端和小程序语法检查通过
    - 覆盖需求 R5.2、R5.3
