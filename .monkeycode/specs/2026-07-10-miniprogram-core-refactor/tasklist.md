# 需求实施计划

- [x] 1. 建立核心场景数据模型和本地服务
  - [x] 1.1 创建第一动作场景配置模块
    - 新增 `miniprogram/utils/core-action-scenes.js`，定义 6 个高频场景、年龄选项、表现选项、默认卡点判断和行动建议
    - 覆盖 `写作业坐不住`、`绘本读两页就跑`、`吃饭拖拉`、`睡前崩溃`、`出门上课磨蹭`、`说话表达弱`
    - 覆盖需求 R2.1、R2.2、R2.3、运营需求 2.2、4.1、11
  - [x] 1.2 实现第一动作结果归一化函数
    - 在 `core-action-scenes.js` 中输出统一 `FirstActionResult` 结构，包含 `sceneKey`、`ageGroup`、`symptomKey`、`bottleneckTitle`、`actionTitle`、`actionSteps`、`createdAt`、`saved`、`completed`
    - 对缺年龄、缺表现、缺场景提供可展示兜底文案
    - 覆盖设计 Data Models: First Action Result，正确性属性 2、4，需求 R2.2、R2.3、R5.5
  - [x] 1.3 实现今晚小任务本地存储服务
    - 新增 `miniprogram/utils/core-action-storage.js`，封装保存、读取最近任务、更新效果记录、读取连续记录次数
    - 使用 `wx.getStorageSync` 和 `wx.setStorageSync`，保持未登录用户可用
    - 覆盖需求 R2.5、R3.1、R3.3、R4.4、运营需求 5.1、5.2、5.3
  - [x]* 1.4 为第一动作结果归一化编写单元测试
    - 覆盖 6 个场景、4 个年龄段、缺年龄、缺表现和未知场景输入
    - 覆盖需求 R2.2、R2.3、R5.5，设计正确性属性 2、4
  - [x]* 1.5 为第一动作结果编写属性测试
    - 属性 P1：任意有效场景和年龄输入都必须生成场景、年龄、卡点判断和行动建议
    - 属性 P2：任意结果的 `actionSteps` 至少包含 1 个可执行步骤
    - 覆盖设计正确性属性 2

- [x] 2. 重构首页首屏和入口层级
  - [x] 2.1 改造首页数据结构
    - 修改 `miniprogram/pages/index/index.js`，新增 `coreRefactorState`、`coreScenes`、`homePrimaryCard`、`recentCoreAction`、`showLegacyHomeSections`
    - 保留 `startupSafeMode`、`featureFlags`、原有跳转函数和运行时配置加载逻辑
    - 覆盖需求 R1.1、R1.2、R3.1、R3.2、R5.5，设计 Home Page 组件
  - [x] 2.2 实现首页主卡片优先级决策
    - 在首页实现 `buildHomePrimaryCard()`，按未完成行动、次日记录、无上下文第一动作、最近记录摘要选择主卡片
    - 输出 `primaryCardType`、`reason`、`targetPath`、`targetPayload`
    - 覆盖设计 Data Models: Home Priority Decision，正确性属性 1、3、5，需求 R3.1、R3.2、R3.3
  - [x] 2.3 重写首页 WXML 首屏
    - 修改 `miniprogram/pages/index/index.wxml`，首屏展示主标题、副标题、一个主按钮和 6 个场景快捷入口
    - 将原 `hero`、`status-section`、`daily-action-section`、`development-zone-section` 和 `功能板块` 调整为首屏之后的辅助区域
    - 覆盖需求 R1.1、R1.2、R3.5、R5.1、运营需求 3.2、3.3
  - [x] 2.4 重写首页 WXSS 视觉层级
    - 修改 `miniprogram/pages/index/index.wxss`，为核心主张、场景入口、继续行动卡片和更多工具创建样式
    - 确保移动端首屏优先展示核心主张、主按钮和部分场景入口
    - 覆盖需求 R3.5、R5.1、运营需求 3.2、3.4
  - [x] 2.5 收敛首页更多工具入口
    - 将首页下半部分功能入口压缩为 `问小牛`、`做观察`、`找方法`、`记成长` 4 个工具
    - 将营养、阅读练习、周报、会员入口从首屏移到对应工具或结果页承接
    - 覆盖需求 R5.1、R5.2、R5.3、运营需求 3.4、6.1、6.2
  - [x]* 2.6 为首页优先级决策编写单元测试
    - 覆盖无记录、有未完成行动、有次日待记录、有最近记录、feature flag 关闭等状态
    - 覆盖需求 R3.1-R3.5，设计正确性属性 1、3、5

- [x] 3. 实现第一动作交互流程
  - [x] 3.1 在首页实现场景选择交互
    - 在 `index.js` 中新增 `onCoreSceneTap`、`startCoreActionFlow`，点击场景后进入年龄选择状态
    - 记录 `scene_select` 埋点并保留场景上下文
    - 覆盖需求 R2.1、R6.2、运营需求 4.1、8.1
  - [x] 3.2 实现年龄选择和档案兜底
    - 在首页内嵌年龄选择区域，优先读取当前孩子年龄，缺失时提供 3-4 岁、4-5 岁、5-6 岁、6 岁以上选项
    - 提供轻量补档入口但允许先选择年龄继续
    - 覆盖需求 R2.2、R2.4、设计 Error Handling，运营需求 4.1、4.2
  - [x] 3.3 实现表现选择步骤
    - 根据当前场景渲染表现选项，点击后生成卡点判断结果
    - 记录 `symptom_select` 和 `bottleneck_result_view` 埋点
    - 覆盖需求 R2.3、R6.2、运营需求 4.1、8.1
  - [x] 3.4 实现卡点判断和今晚第一步结果卡
    - 在首页渲染结果标题、解释文案、行动标题、行动步骤、`我今晚试试`、`继续问细节`
    - 首批 6 个场景均能生成结果，其中写作业和睡前场景使用完整定制文案
    - 覆盖需求 R2.3、R2.5、R4.1、R5.4、运营需求 4.2、11
  - [x] 3.5 实现继续问细节跳转
    - 点击 `继续问细节` 跳转到 `pages/chat/chat` 并带上场景、年龄、表现和卡点文本参数
    - 聊天页接收参数后预填或自动形成上下文提示
    - 覆盖需求 R4.2、R4.3、R5.3、运营需求 6.3
  - [x]* 3.6 为第一动作流程编写交互单元测试
    - 验证选择场景、选择年龄、选择表现、生成结果、保存任务的状态流转
    - 覆盖需求 R2.1-R2.5，运营需求 9.1

- [x] 4. 实现今晚小任务保存和次日记录闭环
  - [x] 4.1 实现 `我今晚试试` 保存动作
    - 在首页 `onSaveTonightAction` 中保存当前第一动作结果，更新本地状态并展示保存成功反馈
    - 记录 `tonight_action_save` 埋点
    - 覆盖需求 R2.5、R4.4、R6.2、运营需求 5.1、8.1
  - [x] 4.2 实现首页次日记录入口
    - 读取最近保存任务，当保存日期早于当天且未记录效果时展示 `记录一下效果` 主卡片
    - 记录 `next_day_record_view` 埋点
    - 覆盖需求 R3.1、R3.3、R4.4、运营需求 5.2、8.1
  - [x] 4.3 实现行动效果记录交互
    - 在首页内嵌效果选择，选项为顺利开始了、还是抗拒、写了一点但很慢、没来得及试
    - 保存 `effectKey`、`effectLabel`、`recordedAt` 并标记任务已记录
    - 覆盖需求 R3.3、R4.4、运营需求 5.3
  - [x] 4.4 实现下一步建议展示
    - 根据效果记录生成下一步建议并展示 `next_action_view`
    - 允许用户再次保存下一步为新的今晚小任务
    - 覆盖需求 R4.4、R5.4、运营需求 5.4、8.1
  - [x] 4.5 将第一动作结果写入成长记录入口
    - 复用 `miniprogram/pages/growth-record` 的记录结构或本地记录格式，保存场景、判断、行动建议和效果
    - 未登录时保留本地记录并继续引导后续同步
    - 覆盖需求 R4.4、R5.4、设计 Growth Record 组件，运营需求 6.1
  - [x]* 4.6 为本地存储服务编写单元测试
    - 覆盖保存、读取、更新效果、连续记录次数、异常数据兜底
    - 覆盖需求 R2.5、R4.4，设计正确性属性 5
  - [x]* 4.7 为次日记录规则编写属性测试
    - 属性 P3：任意已保存且未记录的历史行动都必须生成记录入口
    - 属性 P4：任意已记录行动都必须生成下一步建议或完成态
    - 覆盖设计正确性属性 5，运营需求 5.2、5.4

- [x] 5. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 6. 完成 6 个场景的完整内容与承接
  - [x] 6.1 完善写作业坐不住场景
    - 补齐 4 个年龄段、5 个表现选项、卡点判断、今晚第一步、下一步建议
    - 覆盖运营需求 4.2、11，需求 R2.1-R2.5
  - [x] 6.2 完善睡前崩溃场景
    - 补齐睡前洗漱、上床拖延、关灯哭闹、反复要陪、夜间醒来等表现路径
    - 输出卡点判断和今晚第一步
    - 覆盖运营需求 2.2、4.1、11，需求 R2.1-R2.5
  - [x] 6.3 完善绘本读两页就跑场景
    - 补齐阅读抗拒、坐不住、听不懂、只翻图、复述困难等表现路径
    - 结果页承接 `问小牛` 和 `找方法`
    - 覆盖需求 R5.3、运营需求 6.2
  - [x] 6.4 完善吃饭拖拉场景
    - 补齐挑食、拖饭、离桌、饭桌对抗、只吃零食等表现路径
    - 结果页按需承接营养工具，但不在首页首屏直接突出营养入口
    - 覆盖需求 R5.2、R5.3、运营需求 6.1、6.2
  - [x] 6.5 完善出门上课磨蹭场景
    - 补齐早晨拖延、出门哭闹、入园分离、换衣抗拒、收拾困难等表现路径
    - 输出家庭现场可执行步骤
    - 覆盖需求 R2.3、R4.5、运营需求 2.2
  - [x] 6.6 完善说话表达弱场景
    - 补齐开口少、说不清、复述难、只点头、表达情绪困难等表现路径
    - 结果页承接 AI 追问和成长记录
    - 覆盖需求 R4.1、R4.2、运营需求 2.2、6.3
  - [x]* 6.7 为 6 个场景内容完整性编写属性测试
    - 属性 P5：每个场景必须包含年龄选项、表现选项、卡点判断和行动步骤
    - 属性 P6：每个表现选项必须能生成一个结果卡
    - 覆盖需求 R2.1-R2.5，设计正确性属性 2

- [x] 7. 重组内容、AI 和会员承接
  - [x] 7.1 调整 AI 问答定位和入口文案
    - 首页更多工具中将 AI 文案调整为 `继续追问细节`
    - 聊天页接收第一动作上下文，回答围绕年龄、场景、卡点、今天行动、后续观察
    - 覆盖需求 R1.4、R4.1-R4.5、运营需求 6.3
  - [x] 7.2 在结果页推荐相关育儿知识和工具
    - 根据场景结果展示最多 2 个相关支持入口，例如育儿知识、成长观察、营养或能力练习
    - 内容只在结果之后出现，避免首页首屏内容库化
    - 覆盖需求 R5.3、R5.4、运营需求 6.2
  - [x] 7.3 重写会员承接组件
    - 在完成卡点判断、保存今晚任务、提交次日记录、连续记录 2 次后展示会员承接
    - 文案聚焦 `连续 7 天跟踪孩子变化`、`每天一个更适合的小步骤`、`周末看变化`
    - 覆盖运营需求 7.1、7.2、7.3，需求 R4.3、R4.4
  - [x] 7.4 将原功能板块迁移为更多工具和结果承接
    - 保留原测评、育儿知识、营养、阅读练习、成长记录、周报的可达路径
    - 首页首屏不展示并列功能板块
    - 覆盖需求 R5.1、R5.2、R5.5，运营需求 3.3、3.4、6.1
  - [x]* 7.5 为会员承接展示规则编写单元测试
    - 覆盖完成判断后、保存后、次日记录后、连续记录 2 次、会员已开通等状态
    - 覆盖运营需求 7.1、7.2

- [x] 8. 实现核心埋点和后台统计口径修复
  - [x] 8.1 封装首页第一动作埋点方法
    - 在 `index.js` 新增 `trackCoreActionEvent`，统一写入 `module_key: core_action`、`scene_key`、`age_group`、`symptom_key`、`result_id`
    - 覆盖需求 R6.1、R6.2、R6.3，运营需求 8.1
  - [x] 8.2 接入 10 个关键埋点
    - 接入 `home_core_claim_view`、`first_action_entry_click`、`scene_select`、`age_select`、`symptom_select`、`bottleneck_result_view`、`tonight_action_save`、`next_day_record_view`、`action_effect_submit`、`next_action_view`
    - 埋点失败不影响用户流程
    - 覆盖需求 R6.1-R6.5，运营需求 8.1、9.3
  - [x] 8.3 修复后台 AI 用户统计口径
    - 修改 `backend/src/scripts/build-admin-daily-stats.js`，将 `ai_chat_reply` 纳入 `ai_users`
    - 将 `article_ai_followup` 纳入 AI 相关功能统计或知识追问统计
    - 覆盖运营需求 8.3
  - [x] 8.4 修复后台收藏统计口径
    - 将 `favorite` 和 `unfavorite` 事件拆分统计，避免取消收藏被计入收藏量
    - 调整 `admin_daily_content_stats` 写入逻辑或新增字段兼容展示
    - 覆盖运营需求 8.3
  - [x] 8.5 将第一动作纳入功能完成统计
    - 在 `buildFeatureKeySql` 中识别 `core_action`，将 `tonight_action_save` 和 `action_effect_submit` 计入 complete_count
    - 让后台功能完成数能反映新闭环
    - 覆盖需求 R6.4、运营需求 8.2、8.3
  - [x] 8.6 更新管理后台指标展示
    - 修改 `admin-portal/app.js` 和 `admin-portal/styles.css`，展示首页到第一动作点击率、场景选择完成率、结果生成率、今晚小任务保存率、次日记录率、连续 2 次记录率
    - 复用现有 analytics 接口或新增最小聚合接口
    - 覆盖需求 R6.4、运营需求 8.2
  - [x]* 8.7 为统计脚本编写单元测试
    - 覆盖 `ai_chat_reply`、`article_ai_followup`、favorite、unfavorite、tonight_action_save、action_effect_submit 的归类
    - 覆盖运营需求 8.3

- [x] 9. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [x] 10. 实现多日连续计划和周总结承接
  - [x] 10.1 扩展第一动作为 7 天陪伴计划草稿
    - 在保存今晚任务后生成后续 2-7 天的轻量建议草稿
    - 保存在本地记录中，会员入口可承接完整 7 天跟踪
    - 覆盖运营需求 7.2、10 P2，需求 R4.4、R5.4
  - [x] 10.2 实现连续记录 2 次后的首页状态
    - 首页主卡片展示连续记录反馈和下一步建议
    - 触发会员承接 `继续跟踪这个问题`
    - 覆盖运营需求 7.1、8.2，需求 R3.3、R4.4
  - [x] 10.3 改造周总结入口文案和数据承接
    - 周总结优先展示场景行动记录、效果变化和下一周建议
    - 从第一动作记录中生成周总结摘要兜底数据
    - 覆盖运营需求 10 P2，需求 R4.4、R5.4
  - [x]* 10.4 为连续记录计算编写属性测试
    - 属性 P7：任意按日期递增的已记录行动都能计算连续记录次数
    - 属性 P8：连续记录达到 2 次时必须生成会员承接状态
    - 覆盖需求 R4.4，运营需求 8.2

- [x] 11. 增强场景搜索和内容运营后台联动
  - [x] 11.1 将场景搜索入口接入第一动作结果页
    - 从结果页进入场景搜索时携带 `sceneKey`、`ageGroup`、`bottleneckTitle`
    - 搜索结果优先展示同场景内容和结构化锦囊
    - 覆盖需求 R5.3，运营需求 10 P2
  - [x] 11.2 后台增加核心场景数据缺口展示
    - 在内容运营后台展示 6 个核心场景的内容数量、结果模板数量、缺口状态
    - 与现有核心场景覆盖统计兼容
    - 覆盖运营需求 10 P2，需求 R6.4
  - [x] 11.3 支持后台按场景查看第一动作漏斗
    - 新增或扩展管理端接口，按 `scene_key` 聚合曝光、选择、结果、保存、次日记录
    - 管理端展示各场景转化断点
    - 覆盖运营需求 8.2、10 P2
  - [x]* 11.4 为场景漏斗聚合编写单元测试
    - 覆盖不同 `scene_key`、缺失 `scene_key`、重复事件和跨日事件
    - 覆盖运营需求 8.2

- [x] 12. 优化整个小程序 UI 和功能展示一致性
  - [x] 12.1 梳理并统一核心页面视觉层级
    - 优化首页、聊天页、育儿知识、成长记录、营养工具、阅读练习、周报、会员相关页面的标题层级、按钮样式、卡片间距和空态呈现
    - 保持第一动作主链路视觉优先级最高，其他页面使用一致的行动按钮和结果卡样式
    - 覆盖需求 R1.1、R3.5、R5.1、R5.5，运营需求 3.2、3.4、6.1
  - [x] 12.2 修复核心功能入口展示和路由可达性
    - 确认首页更多工具、第一动作结果页、AI 追问、成长记录、育儿知识、营养、阅读练习、周报、会员承接均有明确入口
    - 修复入口文案、图标、按钮状态、跳转参数和空数据时的展示问题
    - 覆盖需求 R5.1、R5.2、R5.3、R5.5，运营需求 6.1、6.2、6.3
  - [x] 12.3 优化移动端适配和内容溢出
    - 检查小程序常用屏宽下的首屏、列表、结果卡、底部按钮和弹窗布局
    - 修复文字换行、按钮遮挡、横向溢出、固定底部区域遮挡内容等展示问题
    - 覆盖需求 R3.5、R5.5，运营需求 9.1、9.3
  - [x] 12.4 补齐核心页面空态、加载态和错误态
    - 为 AI 加载、内容为空、成长记录为空、营养/阅读数据为空、接口失败、配置缺失提供可继续操作的状态展示
    - 确保失败状态仍能回到第一动作、更多工具或本地兜底建议
    - 覆盖设计 Error Handling，需求 R5.5、R6.5，运营需求 9.3
  - [x]* 12.5 为核心页面展示编写快照或结构测试
    - 覆盖首页、第一动作结果、聊天页上下文入口、成长记录空态和会员承接组件
    - 覆盖需求 R5.1-R5.5，运营需求 9.1

- [x] 13. 完成兼容、回滚和全链路验证
  - [x] 13.1 增加首页新版 feature flag
    - 在运行时配置或本地兜底中增加新版首页开关，关闭时回到原首页工具入口
    - 覆盖需求 R6.5，运营需求 9.3
  - [x] 13.2 实现 AI 不可用和推荐失败兜底
    - 当 AI、接口或推荐逻辑失败时，使用 `core-action-scenes.js` 本地场景建议继续展示结果
    - 覆盖设计 Error Handling，需求 R5.5，运营需求 9.3
  - [x] 13.3 保留原有页面路由和关键入口兼容
    - 确认 `goToChat`、`goToAssessment`、`goToParenting`、`goToNutrition`、`goToTextbook`、`goToGrowthRecord`、`goToWeeklyReport` 可正常访问
    - 覆盖需求 R5.2、R5.5，运营需求 3.4、6.1
  - [x] 13.4 执行根目录语法和回归校验
    - 运行 `npm run lint`
    - 修复小程序、后端和后台管理端语法问题
    - 覆盖设计 Test Strategy
  - [x]* 13.5 执行根目录自动测试
    - 运行 `npm test`
    - 根据失败结果修复与本次改造相关的问题
    - 覆盖设计 Test Strategy
  - [x]* 13.6 为首页核心流程补充自动化页面测试
    - 覆盖打开首页、选择场景、选择年龄、选择表现、保存今晚任务、记录效果
    - 覆盖运营需求 9.1

- [x] 14. 建立完整上线保护和重推门槛
  - [x] 14.1 建立完整功能上线门槛
    - 明确新版上线必须同时具备首页入口收敛、6 个高频场景完整结果、今晚小任务、次日记录、AI 追问、成长记录、会员承接、关键埋点和后台统计
    - 将只有本地基础闭环或只完成 P0 的状态视为开发中状态，继续通过 feature flag 隔离
    - 覆盖需求 R1.1-R1.4、R2.1-R2.5、R3.1-R3.5、R4.1-R4.5、R5.1-R5.5、R6.1-R6.5，运营需求 1-11
  - [x] 14.2 实现旧首页和新版首页双轨保护
    - 保留旧首页核心功能可达路径，新增新版首页开关、灰度比例或白名单配置
    - 开关关闭时恢复旧首页展示和旧功能入口，已生成的新本地记录不阻塞旧功能使用
    - 覆盖需求 R6.5，运营需求 9.3
  - [x] 14.3 实现新旧入口映射和回退清单
    - 为原测评、育儿知识、营养、阅读练习、成长记录、周报、会员入口建立新版入口位置和旧版回退位置
    - 确保用户在新版简化首页中仍能进入有深度的功能，只降低使用成本并保留专业深度
    - 覆盖需求 R5.1-R5.5，运营需求 3.4、6.1、6.2、6.3
  - [x] 14.4 完成重推前核心优势展示验收
    - 在首页和第一动作结果页明确呈现小牛育儿核心优势：孩子年龄与家庭场景结合、先判断卡点、给今晚第一步、第二天继续跟踪、沉淀成长记录
    - 避免首页恢复为功能集合页，深度能力通过结果承接和更多工具进入
    - 覆盖需求 R1.1、R1.2、R4.1-R4.5、R5.3、R5.4，运营需求 1、3.2、4.2、5.4、7.1
  - [x] 14.5 补齐重推观测和快速回退配置
    - 后台展示重推后首页曝光、第一动作点击、场景选择、结果生成、任务保存、次日记录、会员承接点击等核心指标
    - 保留运行时配置项，支持不发版关闭新版主链路
    - 覆盖需求 R6.1-R6.5，运营需求 8.1、8.2、9.3

- [x] 15. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
